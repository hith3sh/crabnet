# Database URL (SQLite for local, PostgreSQL for production)
# SQLite: ./twitterbot.db (default, no need to set)
# PostgreSQL: postgresql://user:password@host:5432/dbname
DATABASE_URL=postgresql://username:password@host:5432/twitterbot

# NextAuth secret (optional, for future features)
NEXTAUTH_SECRET=your-secret-key-here

# Node environment
NODE_ENV=development
